#include <stdio.h>
#include <iostream>

using namespace std;

class Ball {
    int x, y, ballNo;
    public:
    Ball(int a, int b, int No){
        x = a;
        y = b;
        ballNo = No;
    }

};

